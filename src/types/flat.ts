export interface FlatRecord {
  id: string
  floor: number
  unitLabel: string
  ownerName: string
  ulpinId: string
}

export interface TunnelRecord {
  id: string
  label: string
  depthMeters: number
}
